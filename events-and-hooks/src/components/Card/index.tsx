import { CardTitle } from "../CardTitle";
import styles from "./Card.module.css";

export const Card = () => {
  return (
    <div className={styles.container}>
      <CardTitle />
      oi
    </div>
  );
};
